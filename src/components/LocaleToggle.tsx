/**
 * LocaleToggle — switch between Japanese and English UI.
 *
 * Re-exported from `src/i18n` so existing callers importing from
 * `./components/LocaleToggle` keep working. The implementation lives at
 * `src/i18n/LocaleToggle.tsx` and is also exported from the `src/i18n` entry.
 */
export { LocaleToggle } from '../i18n';
