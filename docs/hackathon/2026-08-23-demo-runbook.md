# 2026-08-23 デモ当日オペレーション手帳 / Demo Runbook

**対象**: 8/23 ハッカソン審査デモの当日運用。プレゼンター（エンジニア以外も可）が 60 秒で読んで回せるように作った。**この手帳は 8/23 のデモ実行だけに使う。** 永続 Product の仕様は `docs/specs/product/product-scope-invariant.md` を正とする。

**検証対象ビルド**: https://tokyo-mogu-mogu.pages.dev （commit `688610e` = `main` HEAD、2026-08-17 実測）

- 詳細なタップ手順は `docs/hackathon/demo-sequence.md` を参照。この手帳は手順を重複させず、**当日に必要な確定事実・境界・リカバリだけ**を載せる。
- 台本（ナレーション）は `docs/demo-script.md`、失敗時フローは `docs/hackathon/demo-fallbacks.md`、リハーサルは `docs/hackathon/rehearsal-checklist.md`。

---

## 0. デモ直前チェック（30 秒）

1. URL を開く → **ja** 表示になっているか（ヘッダーで `JA` をタップ）。
2. **リセット**: ヘッダーの `デモデータをリセット` をタップ → 表示が `デモデータをリセットしますか？` に変わったら **もう一度タップ**（2 段階確認）。→ 初回ランディングに戻る。
3. ビューポートは **モバイル幅（375px）** を維持（PC なら幅を狭める）。
4. リセットでクリアされるのは `tmm:foodProfile:v1` / `tmm:moguRecent:v1` / `tmm:savedRoutes` / `tmm:nickname:v1` の 4 キー（localStorage）。**`tmm:locale` はクリアされない**ので、ja へ戻すのは手動。

---

## 1. 当日の流れ

デモ順とタップは `docs/hackathon/demo-sequence.md` の表どおり。**当日の実際のボタン文言は下記が正**（`demo-sequence.md` の一部ラベルは旧表記。実測済み）。

| 画面 | 当日の文言（実測） | 確定チェックポイント |
|---|---|---|
| Landing | CTA `食旅をはじめる` | h1 `東京のローカルな食文化を体験しよう。`、`約2分・アカウント不要` |
| Food Profile | `はじめる！` → nickname（例: `ナナミ`）→ `これでお願いします！` → `了解しました` → `保存してつぎへ` | 食事制限は**自由入力なし**（選択肢のみ） |
| Exploration | ①`さっぱりした味` ②`食べる` ③`東京都`+`1時間以内` ④`自然` ⑤`半日` → `結果を見る` | 出発エリアは `東京都` / `周辺` の固定選択（→ 後述の境界） |
| Result | `96%マッチ度` カード | 必ず **東京わさび**。`※ このマッチ度はデモ用のプロトタイプ表示です…` の注記あり |
| Story | `東京わさびの物語を読む` | h1 `東京わさび`、`味わうことが、継承になる` |
| Route | `モデルルートを見る` | h1 `奥多摩わさび紀行`、`デモ用ルート` ラベル |
| Spot | timeline ピン `奥多摩観光案内所` → `➕ 旅程に追加する` | h1 `奥多摩観光案内所` |
| Save | `🔖 この旅程を保存する` | 保存で `tmm:savedRoutes` に書き込み |

**決定論**: どの選択肢の組み合わせでも 96% 東京わさびになる。実測: 正規パスと異なる選択（辛い系・職人に会う・時間は気にしない・1日 等）でも 96% マッチ度に収束。

**補足**: nickname は `tmm:nickname:v1`（localStorage）に保存される（デモリセットで消去）。当日はどちらも気にする必要なし。

---

## 2. 失敗時リカバリ（要点）

詳細は `docs/hackathon/demo-fallbacks.md`。当日はこの 3 つだけ覚える:

1. **ネットワークが切れてもコア journey は継続できる**（実測済み）。ただし**一度開いたページ内で**戻る/進む操作をする。ページを丸ごと再読込（reload）はネットワークが必要なことがあるので避ける。
2. **状態が汚れたら**: ヘッダー `デモデータをリセット` → もう一度タップ → 初回フローからやり直す。リセット後のリロードでクリーンな初回ランディングに戻る（実測）。
3. **Result が出ない**: `/explore/result` を直接開いても、保存状態がなければ `/food-profile` に自動で戻る（実測）。その場合は 5 問をやり直す（決定論なので同じ結果）。

---

## 3. トゥルース境界 チートシート（「言わないこと」）

審査で誇張しない。**画面に表示される文言をそのまま読めば安全。**

| 項目 | 画面に表示される文言 | 言ってはいけないこと |
|---|---|---|
| **マッチ度 96%** | `※ このマッチ度はデモ用のプロトタイプ表示です。実際の適合度や安全性を保証するものではありません。` | 「96%の人が合う」、本物のレコメンドアルゴリズム、適合度の保証 |
| **食事制限** | `このプロトタイプでは、アレルギーや食事制限への対応可否は判断しません。…詳細は現地・店舗に直接ご確認ください。` | 「アレルギー対応を判定する」「制限を考慮しておすすめしている」 |
| **出発エリア・移動時間** | `出発エリア` + `出発エリアと移動時間を選んでください。`（選択肢は `東京都`/`周辺` × 固定時間のみ） | 「住所から検索した」「リアルタイムの交通ルートを計算した」 |
| **ルート** | `デモ用ルート` | 「あなたの回答を反映して旅程を自動生成した」 |

**プロダクト境界（審査で語る場合）**: 奥多摩 × 東京わさびは **8/23 デモの golden path** であり、「唯一の Product outcome」でも「唯一の未来 geography」でもない。durable Product は Tokyo-wide の multi-region × multi-food-culture。`青梅・沢井 × 日本酒` は source-backed の playable slice で、デモ fixture ではない（`demo-sequence.md` 参照）。

---

## 4. 検証済み事実（エビデンス）

2026-08-17、本番ビルド https://tokyo-mogu-mogu.pages.dev に Playwright（chromium、375px）で実測。**8 シナリオ 53 チェック中 52 PASS、ブラウザ console エラー 0**。

- ✅ **正規パス**（ja/375px）: Landing → Food Profile → Exploration → Result(96%) → Story → Route → Spot → Save、全て PASS。リロード後もルート・プロフィール・履歴が復元される。
- ✅ **決定論**: 代替選択パスでも 96% 東京わさび。
- ✅ **デモリセット**: 2 段階確認 → 4 キー全消去 → リロードでクリーン初回表示。
- ✅ **汚染状態**: `tmm:*` を壊れた JSON にしてもクラッシュせず、クリーンなランディング → 復旧 journey が動く。
- ✅ **ディープリンク**（状態あり）: `/explore/result`(あなたに合う食の旅を見つけました！) / `/story/wasabi-okutama`(h1 東京わさび) / `/route`(奥多摩わさび紀行) / `/spot/okutama-tourism-office`(奥多摩観光案内所) 全て PASS。
- ✅ **オーバーフロー**: ja/en/zh-TW 全て `scrollWidth === clientWidth`（375px）。
- ✅ **ロケール**: ja/en/zh-TW の Landing・Result 見出し + 96% 注記 + オーバーフロー全て PASS。
- ✅ **オフライン**: 読み込み済みページ内の戻る/進むで Result → Story → Route が動作（コア journey はローカル同梱）。

---

## 5. 既知の注意（当日ブロッカーではない）

- ヘッダーの `デモデータをリセット` ボタンのタップ領域が 26px と小さい（推奨 44px 未満）。**押せるので運用は問題なし**。当日は「タップ位置が少し小さい」ことだけ留意。直しは非ブロッキング改善として別途。
- オフライン時の**ページ再読込（cold reload）は想定外**: index.html が HTTP キャッシュ不可で、lazy-load チャンクも初回読み込みにネットワークが必要。当日は**開いたまま**進める。`demo-fallbacks.md` の「Spot 外部リンク CTA はネットワークが必要」も同じ理由。
