node_modules
dist
*.local
.DS_Store
*.tsbuildinfo

# Data acquisition raw cache (Issue #175) — large/full downloads, never committed
.data-cache

# Playwright E2E artifacts (Issue #120)
test-results
playwright-report
playwright/.cache

# Secrets (Issue #233) — Figma access token lives in the environment, never in git
.env
.env.*
!.env.example

# local tool state (oh-my-claudecode)
.omc
.claude/worktrees
